---
# One host. Point ansible_host at your box and edit host_vars/<name>.yml.
# The host is named `spark` while ansible_host carries the real address, so
# renaming your machine does not orphan its host_vars file.
all:
  hosts:
    spark:
      ansible_host: spark.local
      ansible_user: vlad
