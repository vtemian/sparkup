# Alerts for the failures nothing else reports.
#
# Managed by Ansible: roles/sparks/files/sparks.yml. Vendored from the sparks
# repo at d47d5b2, where it is authored as monitoring/alerts/sparks.yml. Edit it
# there, copy it here, and re-run the playbook; the copy is what the box loads.
# Kept as a copy rather than fetched at converge time so that provisioning does
# not depend on reaching a git remote.
#
# STATUS: evaluated, not routed. Prometheus loads these rules and you can see
# their state in /rules and query ALERTS{alertname="..."}. There is no
# Alertmanager on this box, so nothing pages, emails or notifies. "Firing" here
# means a series exists that says so, and someone has to look.
#
# sparkup's INSTALL_CLAUDE.md says a non-world-readable .prom fails silently.
# That is wrong: it sets node_textfile_scrape_error 1. The genuinely silent
# failures are below, and the first one is the reason this file exists.
groups:
  - name: sparks
    rules:
      - alert: SparksRunIndexEmpty
        # An empty or headers-only .prom reports scrape_error 0, a FRESH mtime,
        # and zero series. Total data loss with every health signal green.
        # Nothing in node_exporter reports it; this rule is the only defence.
        #
        # CAVEAT: absent() also fires on a fresh box that has simply never
        # completed a run, which is indistinguishable from the box being down.
        # Do not page on this without a "box has run before" precondition.
        expr: absent(sparks_run_info)
        for: 15m
        annotations:
          summary: "The run index is empty or unreadable"

      - alert: SparksTextfileError
        # Catches unparseable, unreadable, timestamped, or missing-newline
        # files. Note there is no `file` label, so this says something broke
        # without saying what.
        #
        # The job label matches the second scrape job in sparkup's
        # roles/monitoring/templates/prometheus.yml.j2, the slow one that
        # collects only the textfile collector. Renaming that job silences this
        # rule, and nothing would report that it had gone quiet.
        expr: node_textfile_scrape_error{job="node_textfile"} > 0
        for: 15m
        annotations:
          summary: "node_exporter could not read a textfile collector file"

      - alert: SparksQueueRunnerStuck
        # The queue's own silent failure, and the reason it publishes a
        # heartbeat at all: the container is up, `docker ps` is green, and jobs
        # are simply never started. Nothing else on the box can tell the
        # difference between a runner that is idle and one that has wedged --
        # both look like a queue with work in it and nothing running.
        #
        # The heartbeat is written on EVERY pass, including passes that find
        # nothing to do and passes taken while a job runs, so it advancing is a
        # statement about the runner rather than about the workload.
        #
        # 10m against a poll of 5s is a wide margin on purpose: a converge that
        # recreates the container is a legitimate gap, and this must not fire
        # every time the box is provisioned.
        expr: time() - sparks_queue_runner_heartbeat_timestamp_seconds > 600
        for: 5m
        annotations:
          summary: "The queue runner has stopped taking passes"

      - alert: SparksQueueBacklog
        # Not an error, and deliberately not urgent: a queue with work in it is
        # a queue doing its job. This is here for the case it is easy to miss --
        # work piling up behind something that is never going to finish.
        #
        # Paired with the rule above rather than replacing it: a stuck runner
        # with an empty queue is still broken, and a deep queue with a healthy
        # runner is fine.
        expr: sparks_queue_depth{state="queued"} > 5
        for: 6h
        annotations:
          summary: "Jobs have been waiting a long time"

      - alert: SparksDuplicateSeries
        # A duplicate series within one file, or across two files with matching
        # HELP, is dropped by the gatherer with scrape_error still 0. This
        # counter is the only place it surfaces.
        #
        # increase(...) > 0 with NO `for`, not rate(...) > 0 with `for: 15m`:
        # a single error keeps rate() positive for exactly 15 minutes, so the
        # pending window expires as the condition does and the alert never
        # fires. increase() over the window catches the one error outright.
        expr: increase(promhttp_metric_handler_errors_total{cause="gathering"}[15m]) > 0
        annotations:
          summary: "node_exporter is dropping duplicate series"
